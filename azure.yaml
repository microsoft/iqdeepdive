# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: iqdeepdive-foundryiq-proj
services:
    agent-foundryiq-api:
        project: src/agent-foundryiq-api
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: AZURE_AI_SEARCH_SERVICE_ENDPOINT
              value: ${AZURE_AI_SEARCH_SERVICE_ENDPOINT}
        kind: hosted
        name: agent-foundryiq-api
        protocols:
            - protocol: responses
              version: 2.0.0
    agent-toolbox-fabriciq:
        project: src/agent-toolbox-fabriciq
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: CUSTOM_FOUNDRY_FABRIC_TOOLBOX_NAME
              value: ${CUSTOM_FOUNDRY_FABRIC_TOOLBOX_NAME=fabric-ontology-tools}
        kind: hosted
        name: agent-toolbox-fabriciq
        protocols:
            - protocol: responses
              version: 2.0.0
    agent-foundryiq-mcp:
        project: src/agent-foundryiq-mcp
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: AZURE_AI_SEARCH_SERVICE_ENDPOINT
              value: ${AZURE_AI_SEARCH_SERVICE_ENDPOINT}
        kind: hosted
        name: agent-foundryiq-mcp
        protocols:
            - protocol: responses
              version: 2.0.0
    agent-toolbox-foundryiq:
        project: src/agent-toolbox-foundryiq
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: CUSTOM_FOUNDRY_AGENT_TOOLBOX_NAME
              value: ${CUSTOM_FOUNDRY_AGENT_TOOLBOX_NAME=hr-agent-tools}
        kind: hosted
        name: agent-toolbox-foundryiq
        protocols:
            - protocol: responses
              version: 2.0.0
    agent-toolbox-foundryiq-workiq:
        project: src/agent-toolbox-foundryiq-workiq
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: CUSTOM_FOUNDRY_AGENT_TOOLBOX_NAME
              value: ${CUSTOM_FOUNDRY_WORKIQ_KB_TOOLBOX_NAME=workiq-knowledge-tools}
        kind: hosted
        name: agent-toolbox-foundryiq-workiq
        protocols:
            - protocol: responses
              version: 2.0.0
    agent-toolbox-workiq:
        project: src/agent-toolbox-workiq
        host: azure.ai.agent
        language: python
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: main.py
            runtime: python_3_13
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        environmentVariables:
            - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
              value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            - name: CUSTOM_FOUNDRY_WORKIQ_TOOLBOX_NAME
              value: ${CUSTOM_FOUNDRY_WORKIQ_TOOLBOX_NAME=work-iq-tools}
        kind: hosted
        name: agent-toolbox-workiq
        protocols:
            - protocol: responses
              version: 2.0.0
hooks:
    postdeploy:
        windows:
            shell: pwsh
            run: infra/hooks/postdeploy.ps1
            interactive: true
        posix:
            shell: sh
            run: infra/hooks/postdeploy.sh
            interactive: true
    postprovision:
        windows:
            shell: pwsh
            run: infra/hooks/postprovision.ps1
            interactive: true
        posix:
            shell: sh
            run: infra/hooks/postprovision.sh
            interactive: true
